substitutions:
  device_name: family-esp32

sensor:
  - platform: uptime
    name: "${device_name} Uptime Sensor"

  - platform: wifi_signal
    name: "${device_name} WiFi Signal"
    update_interval: 60s

<<: !include base/mhet-devkit-ble.yaml
